# Claude Code Instructions for SpecForge

## Python Environment

Use `uv` to run Python commands. This ensures the correct virtual environment and dependencies are used.

```bash
# Run Python scripts
uv run python script.py

# Run Python modules
uv run python -m module_name
```
